# أقم — Aqim

مدرب شخصي لبناء عادة الصلاة تدريجيًا، أسبوعًا بعد أسبوع، بدل تطبيق مواقيت عادي.

هذا مشروع Flutter كامل (Android) جاهز للتجريب على هاتف حقيقي. النسخة الحالية
تستعمل بيانات محلية فقط (SharedPreferences) — لا يوجد خادم أو حساب مستخدم بعد.

## أسرع طريقة للتجريب: عبر GitHub Actions (بدون تثبيت Flutter)

1. أنشئ مستودعًا جديدًا على GitHub وارفع هذا المجلد إليه.
2. بمجرد الدفع (push) إلى فرع `main`، سيعمل الووركفلو `Build Aqim APK`
   تلقائيًا (Actions → Build Aqim APK).
3. بعد انتهاء التشغيل بنجاح، افتح تبويب Artifacts وحمّل `aqim-debug-apk`.
4. انقل ملف `app-debug.apk` إلى هاتفك (عبر الإيميل، Google Drive، أو كابل
   USB) وثبّته يدويًا — قد تحتاج للسماح بـ"تثبيت من مصادر غير معروفة" في
   إعدادات هاتفك.

## التجريب محليًا (إن كان Flutter مثبتًا على جهازك)

```bash
flutter pub get
flutter run          # يوصل الهاتف عبر USB مع تفعيل خيارات المطور + تصحيح USB
# أو لبناء ملف APK مباشرة:
flutter build apk --debug
# الملف الناتج: build/app/outputs/flutter-apk/app-debug.apk
```

## بنية المشروع

```
lib/
  main.dart                 # نقطة الدخول + تهيئة Google Mobile Ads
  ads/                       # معرّفات AdMob + بانر إعلاني قابل لإعادة الاستخدام
  theme/app_theme.dart       # ألوان وخطوط أقم (نيلي الليل، ذهبي الفجر)
  models/prayer.dart         # الصلوات الخمس، الآيات/الأحاديث، أسباب الفوات
  models/adhkar.dart         # أذكار ما قبل وما بعد الصلاة كاملة
  services/audio_service.dart # تشغيل التسجيلات الصوتية (تُضاف لاحقًا)
  services/location_service.dart      # صلاحية الموقع + إحداثيات الهاتف
  services/prayer_times_service.dart  # جلب أوقات الصلاة الحقيقية (AlAdhan API)
  services/mosque_service.dart        # أقرب المساجد (Overpass/OpenStreetMap)
  services/geocoding_service.dart     # اسم المدينة من الإحداثيات (Nominatim)
  services/offline_prayer_times_service.dart # حساب الأوقات محليًا (بلا حزمة خارجية)
  services/purchase_service.dart      # شراء إزالة الإعلانات (Google Play Billing)
  utils/hijri_date.dart                # تحويل ميلادي→هجري (خوارزمية جدولية، بلا حزمة)
  utils/gregorian_arabic.dart          # تنسيق التاريخ الميلادي بأسماء عربية
  services/notification_service.dart  # تذكيرات قبل/بعد كل صلاة + ملخص أسبوعي
  services/battery_service.dart       # فحص/طلب استثناء توفير البطارية
  navigation/nav_key.dart              # مفتاح تنقّل عام لفتح شاشة من إشعار
  state/app_state.dart       # منطق الأسبوع التدريجي، السلسلة، سجل الأسبوع
  widgets/day_arc.dart       # عنصر "قوس اليوم" المميز (CustomPainter)
  screens/
    onboarding_screen.dart   # هدف الأسبوع الأول
    home_screen.dart         # قوس اليوم + الصلاة القادمة + رسالة الهوية
    pre_prayer_screen.dart   # آية / حديث / فائدة + رابط أذكار ما قبل الصلاة
    dhikr_screen.dart        # عداد الذكر السريع (٣٣/٣٣/٣٤) + رابط الأذكار الكاملة
    adhkar_flow_screen.dart  # شاشة عامة تعرض أي قائمة أذكار خطوة بخطوة
    identity_screen.dart     # تعزيز الهوية بعد إتمام الصلاة
    reason_screen.dart       # "لماذا لم أُصلِّ" + اقتراح مخصص
    week_report_screen.dart  # لوحة الحياة الأسبوعية
    settings_screen.dart     # توقيت التذكيرات القابل للتخصيص + زر البطارية
    nearby_mosques_screen.dart # أقرب المساجد + زر الاتجاهات
```

## إضافة التسجيلات الصوتية لاحقًا

كل ذكر في التطبيق له زر 🔊 يشغّل تسجيلًا صوتيًا. الأزرار جاهزة الآن لكنها
ستُظهر رسالة "لم يُضَف التسجيل بعد" حتى تضيف الملفات التالية بنفس الأسماء
تمامًا (صيغة mp3) داخل هذين المجلدين:

`assets/audio/before/` (أذكار ما بين الأذان والإقامة):
- ✅ `tarid_athan.mp3`
- ✅ `dua_athan.mp3`
- ✅ `dua_bayn_athan_iqama.mp3`

`assets/audio/after/` (أذكار ما بعد الصلاة):
- ✅ `istighfar.mp3`
- ✅ `sayid_salam.mp3`
- ✅ `tahlil_mulk.mp3`
- ✅ `tasbih.mp3`
- ✅ `tahmid.mp3`
- ✅ `takbir.mp3`
- ✅ `tahlil_complete.mp3`
- ✅ `ayat_kursi.mp3`
- ✅ `al_ikhlas.mp3`
- ✅ `al_falaq.mp3`
- ✅ `an_nas.mp3`

بعد إضافة الملفات، شغّل `flutter pub get` مرة أخرى (لإعادة تسجيل الأصول)
ثم أعد بناء التطبيق.

## إزالة الإعلانات (شراء لمرة واحدة)

شاشة الإعدادات فيها بطاقة "إزالة الإعلانات نهائيًا" — دفعة واحدة (بلا
اشتراك) عبر Google Play Billing، تُخفي البانر الإعلاني من كل الشاشات
مدى الحياة بعد الشراء. فيها زر "استعادة المشتريات" أيضًا لمن يُثبّت
التطبيق على جهاز جديد.

### ⚠️ خطوات إلزامية قبل ما يشتغل الشراء

الكود جاهز بالكامل، لكن **الشراء لن يعمل إطلاقًا** حتى تُنشئ المنتج فـ
Google Play Console:

1. ارفع التطبيق (حتى لو فـ مسار "الاختبار الداخلي" Internal Testing) —
   هذا **إلزامي**: الشراء الحقيقي لا يعمل أبدًا مع APK مُثبَّت يدويًا
   (مثل الـ APK القادم من GitHub Actions حاليًا)، فقط مع تطبيق مثبَّت
   عبر Google Play (ولو فـ مسار اختبار).
2. Play Console → تطبيقك → **Monetize → Products → In-app products**.
3. أنشئ منتجًا جديدًا بمعرّف **مطابق بالضبط** لهذا:
   ```
   remove_ads_lifetime
   ```
   (معرّف مكتوب فـ `lib/services/purchase_service.dart` — إن غيّرته هناك
   غيّره هنا بنفس القيمة بالضبط).
4. اختر نوع المنتج: **منتج مُدار (Managed product)** — أي غير قابل
   للاستهلاك، يُشترى مرة واحدة فقط. **ماشي** اشتراك (Subscription).
5. حدّد السعر: **19 دولار أمريكي** (أو ما يعادله بعملات أخرى — Google
   تحسب التحويل تلقائيًا). السعر المعروض فـ التطبيق يُقرَأ مباشرة من
   Play Console (`state.removeAdsPriceLabel`)، فلا حاجة لتغيير أي كود
   عند تعديل السعر لاحقًا.
6. فعّل المنتج (Activate).
7. أضف حسابك (أو حسابات المختبرين) كـ **License Testers** فـ Play
   Console (Setup → License testing) للشراء التجريبي بلا دفع حقيقي أثناء
   التطوير.

### ملاحظات

- بلا هذه الخطوات، الزر موجود لكن سيظهر خطأ "المنتج غير متوفر حاليًا"
  عند الضغط عليه — هذا متوقّع وليس خطأ فـ الكود.
- التحقق من الشراء حاليًا من جانب التطبيق فقط (client-side) — كافٍ
  لتطبيق فردي صغير، لكن لتفادي أي تلاعب فـ نسخ مُعدَّلة من الـ APK
  لاحقًا، الخطوة التالية الموصى بها هي تحقق من جانب الخادم (Google Play
  Developer API) — غير مُنفَّذ حاليًا.

## الإعلانات (AdMob)

التطبيق مربوط بـ Google Mobile Ads SDK وفيه بانر إعلاني في شاشتين فقط:
**الرئيسية** و**التقرير الأسبوعي** — بشكل مقصود لا يوجد أي إعلان في شاشات
الصلاة أو الأذكار أو الهوية، احترامًا لطبيعة المحتوى.

**الوضع الحالي**: يستعمل معرّفات اختبار رسمية من Google (`ca-app-pub-3940256099942544/...`)
— تعمل فورًا بلا أي إعداد إضافي، لكن بلا أي عائد مالي حقيقي.

**قبل النشر على المتجر، خاصك:**
1. أنشئ حسابًا على [AdMob](https://admob.google.com) وسجّل تطبيق أقم فيه.
2. استبدل معرّف التطبيق فـ
   `android/app/src/main/AndroidManifest.xml` (بحث عن `APPLICATION_ID`)
   بمعرّف تطبيقك الحقيقي.
3. استبدل معرّف البانر فـ `lib/ads/ad_ids.dart` بمعرّف وحدة الإعلان
   الحقيقي.
4. انتظر مراجعة Google للتطبيق (عادة تستغرق ساعات لأول مرة) قبل أن تظهر
   إعلانات حقيقية بدل رسالة "لا يوجد إعلان متاح".

## مواقيت الصلاة الحقيقية

التطبيق يجلب الآن أوقات الصلاة الفعلية بدل الأوقات الوهمية:
- يطلب صلاحية الموقع عند أول تشغيل (يحتاجها لحساب الأوقات بدقة).
- يستعمل [AlAdhan API](https://aladhan.com) بطريقة حساب وزارة الأوقاف
  والشؤون الإسلامية المغربية (method=21) كمصدر أساسي.
- يعمل في الخلفية بلا حجب شاشة الإقلاع — الأوقات تظهر وتتحدّث في "قوس
  اليوم" وبطاقة "الصلاة القادمة" بمجرد توفّرها.
- **يعمل بلا إنترنت بالكامل**: إن تعذّر الوصول لـ AlAdhan API، يحسب
  التطبيق الأوقات **محليًا على الهاتف** عبر معادلات فلكية قياسية مكتوبة
  بالكامل داخل المشروع (خوارزمية praytimes.org المعروفة عالميًا، طريقة
  رابطة العالم الإسلامي) — **بلا أي حزمة خارجية**، لتفادي مفاجآت تغيّر
  واجهات الحزم بين الإصدارات. يكفي توفّر الموقع فقط (GPS يعمل بلا شبكة).
  الفرق عن الطريقة الرسمية عادة دقائق قليلة، وتظهر ملاحظة بذلك فـ شاشة
  الإعدادات.
- آخر إحداثيات معروفة تُحفَظ محليًا (SharedPreferences) وتُستعمل كبديل
  إن فشل تحديد الموقع في مرة لاحقة لكنه نجح سابقًا.
- **اسم المدينة**: يُحدَّد عبر [Nominatim](https://nominatim.org)
  (OpenStreetMap، بلا مفتاح API) ويظهر فـ الشاشة الرئيسية وشاشة
  الإعدادات — تأكيد بصري بسيط أن التطبيق حدّد موقعك صح. يحتاج إنترنت (لا
  يعمل بلا شبكة، بخلاف حساب الأوقات نفسه).
- **التاريخ اليوم**: الشاشة الرئيسية تعرض التاريخ الميلادي والهجري معًا
  (تحويل رياضي بالخوارزمية الجدولية المعروفة، بلا حزمة خارجية ولا
  إنترنت) — للتتبع اليومي المباشر.

### ما يعمل بلا إنترنت وما لا يعمل

| الميزة | يعمل بلا إنترنت؟ |
|---|---|
| حساب أوقات الصلاة | ✅ نعم (بعد أول تشغيل ناجح، أو بموقع GPS مباشر) |
| منبّه الاستعداد + الأذان + تذكير "هل صليت؟" | ✅ نعم (إشعارات محلية بمجرد جدولتها) |
| أذكار ما قبل/بعد الصلاة + الصوت | ✅ نعم (كلها مُخزَّنة داخل التطبيق) |
| اسم المدينة | ❌ يحتاج إنترنت (Nominatim) |
| أقرب مسجد | ❌ يحتاج إنترنت (Overpass) |
| الإعلانات (AdMob) | ❌ لن تظهر بلا إنترنت (طبيعي) |
| تفعيل/استعادة شراء إزالة الإعلانات | ❌ يحتاج إنترنت واتصال بـ Google Play |

### تشخيص "الإشعارات لا تعمل"

سبب شائع لعدم ظهور أي إشعار: **فشل جلب الأوقات الحقيقية بصمت** (موقع
مرفوض، أو بلا إنترنت وقت أول تشغيل) — بما أن الإشعارات تُجدوَل فقط على
أوقات حقيقية، فشل الجلب يعني عدم جدولة أي شيء إطلاقًا، ولا حتى تغيير
الإعدادات كان يُصلحه سابقًا. تم إصلاح هذا بالكامل:

- شاشة الإعدادات تعرض الآن بطاقة حالة واضحة أعلاها: **"الإشعارات
  مفعّلة"** (أخضر) مع اسم المدينة، أو **"الإشعارات غير مفعّلة"** (أحمر)
  مع السبب الدقيق وزر **"إعادة المحاولة"**.
- نفس التنبيه يظهر بشكل مختصر أعلى الشاشة الرئيسية إن كانت الإشعارات
  غير مفعّلة — اضغط عليه للتفاصيل.
- تغيير أي إعداد (توقيت التذكير، تفعيل الأذان) الآن **يحاول تفعيل
  الإشعارات من جديد تلقائيًا** إن لم تكن مفعّلة بعد، بدل تجاهل اختيار
  المستخدم بصمت كما كان يحدث سابقًا.

إن ظهرت "الإشعارات غير مفعّلة" باستمرار: تأكد من تفعيل خدمة الموقع
(GPS) وصلاحية الموقع للتطبيق، وأن الإنترنت متصل، ثم اضغط "إعادة
المحاولة" فـ الإعدادات.

**ملاحظة**: منطق التقدّم عبر الصلوات (أيّها "القادمة" الآن) يبقى تسلسليًا
بحسب ما أكمله المستخدم فعليًا، وليس مرتبطًا بالساعة الفعلية — الأوقات
الحقيقية هنا للعرض والتذكير فقط. هذا خيار تصميم مقصود يسمح للمستخدم
بمتابعة صلواته بترتيبها الطبيعي حتى لو فتح التطبيق متأخرًا عن وقتها.

## تذكيرات الصلاة (إشعارات حقيقية)

التطبيق يرسل **ثلاثة** تنبيهات لكل صلاة من الصلوات الخمس، بشرط توفّر
الأوقات الحقيقية (لا يُجدوَل أي إشعار على الأوقات الاحتياطية الوهمية):

1. **منبّه الاستعداد** (10 دقائق قبل الوقت، قابل للتخصيص): "استعد لصلاة
   [الصلاة]" — منبّه حقيقي بصوت مخصّص، يعمل حتى والهاتف مقفل أو صامت
   (شاشة كاملة + فئة "منبّه" فـ أندرويد)، لمساعدتك على النهوض والاستعداد
   — خصوصًا للفجر. الضغط عليه يفتح شاشة الآية/الحديث/الفائدة.
2. **الأذان**: عند دخول وقت الصلاة بالضبط، إشعار بصوت الأذان (إن أضفته
   — راجع "إضافة أصوات المنبّه والأذان" أدناه).
3. **هل صليت؟** (20 دقيقة بعد الوقت، قابل للتخصيص): بزر إجراء **"أجّل 15
   دقيقة"** يُعيد جدولة نفس الإشعار بلا فتح التطبيق.
4. **ملخّص أسبوعي**: كل جمعة الساعة ٨ مساءً، "أتممت X من ٣٥ صلاة هذا
   الأسبوع 🌙".

**إعداد أول مرة**: عند أول فتح للتطبيق، بعد شاشة الهدف، يمر المستخدم
بخطوة "كيف تريد أن يعمل التطبيق؟" يختار فيها مباشرة: توقيت منبّه
الاستعداد، توقيت تذكير "هل صليت؟"، تفعيل/تعطيل صوت الأذان، وفتح إعدادات
البطارية إن أراد — كل هذا قابل للتغيير لاحقًا فـ أي وقت من شاشة
الإعدادات (⚙️).

**تخصيص التوقيت**: من شاشة الإعدادات يقدر المستخدم يختار توقيت منبّه
الاستعداد (5 إلى 30 دقيقة) وتذكير "هل صليت؟" (10 إلى 45 دقيقة) — يُعاد
جدولة كل الإشعارات فورًا عند التغيير.

**تعطيل صوت الأذان**: مفتاح فـ الإعدادات (وفـ الإعداد الأول) يسمح بإيقاف
إشعار الأذان عند وقت الصلاة فقط — منبّه الاستعداد وتذكير "هل صليت؟"
يبقيان فعّالين دائمًا بغض النظر عن هذا الخيار.

**استثناء توفير البطارية**: يمكن فتحه من خطوة الإعداد الأول أو لاحقًا
من شاشة الإعدادات فـ أي وقت (تشمل إعدادات خاصة بالشركة المصنّعة على
أجهزة Xiaomi/Huawei/Oppo وغيرها، وهي غالبًا السبب الرئيسي لتأخّر أو
اختفاء التذكيرات على هذا النوع من الهواتف).

### إضافة أصوات المنبّه والأذان

**مهم: هذه الأصوات لا تُضاف فـ `assets/audio/` كباقي التسجيلات** —
أصوات الإشعارات على أندرويد تحتاج مكانًا خاصًا داخل المشروع الأصلي:

```
android/app/src/main/res/raw/wakeup_alarm.mp3   ← صوت منبّه الاستعداد
android/app/src/main/res/raw/adhan.mp3          ← صوت الأذان
```

ملاحظات مهمة عن هذين الملفين تحديدًا:
- اسم الملف يجب أن يكون بأحرف إنجليزية صغيرة وأرقام وشرطة سفلية `_` فقط
  (بدون مسافات أو شرطات `-`) — قيد من أندرويد نفسه.
- لـ `adhan.mp3`: يُفضَّل مقطع قصير (٢٠-٤٠ ثانية) بدل الأذان الكامل،
  لضمان تشغيله بشكل موثوق كصوت إشعار على مختلف الأجهزة.
- **تنبيه حقوق النشر**: إن استعملت تسجيل أذان لمؤذّن معروف (كالحرم
  المكي مثلاً)، تأكد من أن الترخيص يسمح بإعادة الاستعمال داخل تطبيق،
  أو سجّل أذانًا خاصًا بك — الأصوات المتداولة على الإنترنت ليست كلها
  حرة الاستعمال.
- إن لم تُضَف الملفات، يعمل كل شيء بلا كراش — تُستعمل نغمة الإشعار
  الافتراضية للنظام بدلاً منها.
- بعد إضافة الملفين، لا حاجة لـ `flutter pub get` (لأنهما موارد أندرويد
  أصلية وليست Flutter assets) — أعد البناء مباشرة.
- **حالة هذا المشروع الآن**: الملفان مُضافان فعلًا (`wakeup_alarm.mp3`
  ~34 ثانية، و`adhan.mp3` ~4 دقائق كاملة). الأذان الكامل قد يُقطَع أو
  يتصرّف بشكل مختلف على بعض الأجهزة كصوت إشعار طويل نسبيًا — إن لاحظت
  ذلك أثناء التجريب، جرّب استبداله بمقطع أقصر (٢٠-٤٠ ثانية) من نفس
  التسجيل.

## أقرب مسجد

زر "أقرب مسجد" فـ الشاشة الرئيسية يعرض قائمة بأقرب المساجد (ضمن 3 كم)
مرتّبة حسب المسافة، مع زر "الاتجاهات" يفتح خرائط Google للتوجّه إليها.

- يستعمل بيانات **OpenStreetMap** عبر Overpass API — مصدر مفتوح ومجاني
  بلا حاجة لمفتاح API أو فوترة (بخلاف Google Places API).
- **قيد معروف**: تغطية OpenStreetMap للمساجد تعتمد على مساهمات
  المستخدمين، فقد تكون غير كاملة فـ بعض المناطق (خصوصًا القرى الصغيرة).
  المساجد المسجَّلة فـ المدن الكبرى فـ المغرب عادة موثّقة بشكل جيد.
- يستعمل آخر إحداثيات معروفة (نفس المصدر المستعمل لأوقات الصلاة) إن
  تعذّر تحديد الموقع الحالي فورًا.

### ملاحظات تقنية عن الإشعارات
- تُعاد جدولة كل الإشعارات (منبّه الاستعداد + الأذان + الملخّص الأسبوعي)
  كل مرة تُجلَب فيها أوقات جديدة بنجاح (عادة مرة عند فتح التطبيق يوميًا).
  فتح التطبيق يوميًا ضروري حاليًا لتحديث الإشعارات لليوم الجديد — لا
  توجد آلية لإعادة الجدولة تلقائيًا في الخلفية أو بعد إعادة تشغيل
  الهاتف بعد.
- تُستعمل جدولة "غير دقيقة" (`inexactAllowWhileIdle`) عمدًا، فقد تتأخر
  الإشعارات بضع دقائق أحيانًا (خصوصًا فـ وضع توفير الطاقة). هذا اختيار
  مقصود لتفادي طلب صلاحية "المنبّهات الدقيقة" الحساسة على أندرويد 12+
  (`SCHEDULE_EXACT_ALARM`)، والتي قد تُعقّد مراجعة Google Play لاحقًا.
- زر "أجّل 15 دقيقة" لا يعمل إن كان النظام قد أغلق التطبيق بالكامل من
  الذاكرة (وليس فقط فـ الخلفية) — يحتاج ذلك معالجة إضافية عبر background
  isolate غير مُنفَّذة حاليًا؛ الضغط عليه فـ تلك الحالة سيفتح التطبيق
  بدل تأجيل الإشعار بصمت.
- يحتاج Android 13+ صلاحية إشعارات صريحة — يطلبها التطبيق تلقائيًا عند
  أول تشغيل.
- تتطلب `flutter_local_notifications` تفعيل *core library desugaring*
  فـ `android/app/build.gradle` (مُفعَّل مسبقًا فـ هذا المشروع).
- **صلاحية `USE_FULL_SCREEN_INTENT`** (لمنبّه الاستعداد): على أندرويد
  14+، قد يرفض Google Play منح هذه الصلاحية تلقائيًا للتطبيقات إلا إن
  صرّحت بامتلاكها وظيفة "منبّه" حقيقية عند النشر على المتجر — راجع
  [توثيق Google](https://source.android.com/docs/core/permissions/fsi-limits)
  قبل النشر. لا يؤثر هذا على التجريب المحلي أو عبر GitHub Actions.

## ما تم تبسيطه في هذه النسخة (ولخطوات لاحقة)

- **إعادة الجدولة التلقائية**: الإشعارات تُجدوَل عند فتح التطبيق فقط.
  لجعلها تُحدَّث تلقائيًا كل يوم بلا فتح التطبيق، ستحتاج مهمة خلفية
  (WorkManager) — خطوة إضافية غير مُنفَّذة حاليًا.
- **الصوت**: أزرار 🔊 موجودة على كل ذكر وتعمل فعليًا (عبر `audioplayers`)،
  لكنها بلا تسجيلات حتى الآن — راجع قسم "إضافة التسجيلات الصوتية" أعلاه.
- **التوقيع للنشر على المتجر**: الإصدار الحالي (`--release`) موقّع بمفتاح
  Debug مؤقتًا كي يُبنى بدون إعداد إضافي. قبل رفعه إلى Play Store، أنشئ
  keystore حقيقي واربطه عبر GitHub Secrets (نفس الأسلوب المستعمل في "سُفرة").
- **الخطوط**: تُحمَّل عبر `google_fonts` عند أول تشغيل (تتطلب إنترنت مرة
  واحدة، ثم تُخزَّن محليًا).

## الخطوة التالية المقترحة

بعد تجربة التدفق على الهاتف، أخبرني بأي تعديل تريده (مثلًا: ربط مواقيت
صلاة حقيقية، إضافة إشعارات، أو البدء في نسخة قصص ما بعد الصلاة)، ونكمل من
هناك.
