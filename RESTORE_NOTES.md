# Restore notes

This file records the requested targeted restoration:
- restore missed-prayer notifications (`فاتتك صلاة ...`) to the previous behavior;
- keep `هذا الأسبوع` driven by actual prayer completion/history;
- remove the underline/indicator shown beneath bottom-menu labels only.

No other app behavior should be changed.
