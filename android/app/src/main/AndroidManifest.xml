<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- يحتاجها مكتبة google_fonts لتنزيل الخطوط (Amiri, Cairo, Tajawal) عند أول تشغيل -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- يحتاجها google_mobile_ads للوصول لمعرّف الإعلانات على أندرويد 13+ -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />
    <!-- يحتاجها geolocator لحساب أوقات الصلاة الحقيقية بناءً على الموقع -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- يحتاجها flutter_local_notifications لعرض تذكيرات الصلاة على أندرويد 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- تسمح بطلب استثناء التطبيق من توفير البطارية كي تصل التذكيرات فوقتها -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- يحتاجها منبّه ما قبل الصلاة (يعمل حتى والهاتف مقفل، كمنبّه حقيقي) -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- تحتاجها in_app_purchase للتواصل مع Google Play Billing -->
    <uses-permission android:name="com.android.vending.BILLING" />
    <application
        android:label="أقم"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!--
          معرّف تطبيق AdMob — هذا معرّف اختبار رسمي من Google (يعمل دائمًا،
          بلا عائد حقيقي). استبدله بمعرّف تطبيقك الفعلي من حساب AdMob قبل
          النشر (راجع lib/ads/ad_ids.dart لنفس الملاحظة على معرّفات البانر).
        -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
