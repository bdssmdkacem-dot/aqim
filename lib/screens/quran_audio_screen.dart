import 'package:flutter/material.dart';
class QuranAudioScreen extends StatelessWidget { const QuranAudioScreen({super.key}); @override Widget build(BuildContext context)=>const Scaffold(body:Center(child:Text('Quran audio'))); }
